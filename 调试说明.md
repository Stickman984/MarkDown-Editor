# 调试说明

## 已添加调试日志

我已经在代码中添加了详细的调试信息，现在请执行以下步骤：

### 1. 重新启动应用（从命令行）

请在命令行中运行：
```bash
D:\Data_Rufei\Python312\python.exe md_viewer.py README.md
```

**重要**: 不要关闭命令行窗口，日志会在那里显示

### 2. 点击链接

在应用中点击以下测试链接：
- `[启动查看器.bat](./启动查看器.bat)`
- `[requirements.txt](./requirements.txt)`
- `[md文件](./README.md)`

### 3. 查看日志输出

命令行窗口会显示类似这样的调试信息：

```
[DEBUG] CustomHTMLScrolledText.__init__ called
[DEBUG] viewer: <__main__.MarkdownViewer object at 0x...>
[DEBUG] Before override, on_link_click type: <class 'method'>
[DEBUG] After override, on_link_click type: <class 'method'>

===== 当点击链接时 =====
[DEBUG] ===== _custom_link_click called =====
[DEBUG] URL: ./启动查看器.bat
[DEBUG] Has viewer: True
[DEBUG] Calling viewer.handle_link_click(./启动查看器.bat)

[DEBUG] ===== handle_link_click called =====
[DEBUG] Raw URL: ./启动查看器.bat
[DEBUG] Decoded URL: ./启动查看器.bat
[DEBUG] Checking if path exists: d:\Amlogic\Tools\md_preview\启动查看器.bat
[DEBUG] Path exists!
[DEBUG] Is file, extension: .bat
[DEBUG] Text file detected
[DEBUG] Opening with Notepad++: C:\Program Files\Notepad++\notepad++.exe
```

### 4. 将日志发送给我

请将命令行窗口中的所有输出复制给我，特别是：
- 应用启动时的日志
- 点击链接时的日志

这样我就能看到：
1. 自定义的 `_custom_link_click` 方法是否被调用
2. 如果被调用，URL 是什么
3. 如果没被调用，说明覆盖没有生效

## 预期情况

### 情况 A: 看到我们的日志
如果看到 `[DEBUG] _custom_link_click called`，说明我们的代码被调用了，问题可能在于路径处理

### 情况 B: 没有看到我们的日志
如果没有看到任何 `[DEBUG]` 信息，说明：
1. tkhtmlview 的链接处理机制和我们理解的不一样
2. 需要用其他方法来覆盖默认行为


