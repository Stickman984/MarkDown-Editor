# 内存优化

## 优化记录

1. swap

     `[ro.lmk.swap_free_low_percentage]: [15]`

2. logcat buf
    ```
    main ring buffer is 40 MiB (10 MiB consumed, 1 MiB readable), max entry is 5120 B, max payload is 4068 B
    system ring buffer is 40 MiB (10 MiB consumed, 53 KiB readable), max entry is 5120 B, max payload is 4068 B
    crash ring buffer is 40 MiB (32 KiB consumed, 0 B readable), max entry is 5120 B, max payload is 4068 B
    kernel ring buffer is 40 MiB (0 B consumed, 0 B readable), max entry is 5120 B, max payload is 4068 B
    再查看又正常了？？？。
    ```

1. /proc/sys/vm/watermark_scale_factor
    60

2. 516,         c0b012a0, vbi_probe
https://scgit.amlogic.com/#/c/411055/ 512分支应该已经合并，需要确认下为啥没生效

1. 客户app
   `8,169K: com.dangbei.needle (pid 1422)`
   `4,169K: com.stm.factorytest`
   `22,855K: com.dangbei.lerad.vesuvius.sample`

2. 输入法 
   6,861K: com.droidlogic.inputmethod.remote
   https://scgit.amlogic.com/#/c/435456/ 512M分支应该已经合并
   看下这个文件device/amlogic/t950s_be311/t950s_be311.mk有没有包含
   PRODUCT_PACKAGES += \
   LowRamOverlay

3. kerneleventfs 优化
    https://scgit.amlogic.com/#/c/568646/

4. dm pool优化
    <https://source.amlogic.com/c/tv/customer/ay30a5/kernel/common/+/71486>

5. 1,remove devicetree node 2,reduce kernel code size 
   https://source.amlogic.com/c/tv/customer/ay30a5/kernel/common/+/71483

6.   20000,         bf34bd7c, aicwf_prealloc_init [aic_load_fw]

7.   5836,         c02f83b0, __rb_allocate_pages

8.   debugfs pagetrace user版本去掉

9.  com.dangbei.lerad.vesuvius.sample 资源占用  [trace](./trace/trace-aiqyi-调音量)
    ![alt text](image.png) 

10. com.droidlogic.droidlivetvsettings 继续追查下
    ![alt text](image-1.png)

11. 对下1g的KPI，看下减多少内存可以接受，emmc速度要一致
12. com.android.systemui 参考小米
13. zram调大
14. amlmmc_dtb_init
15. devkmsg_open
16. trace_elems_alloc
17. secmon
   